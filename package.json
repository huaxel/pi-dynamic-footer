{
  "name": "@juanbenjumea/pi-dynamic-footer",
  "version": "0.1.10",
  "description": "Dynamic configurable footer for pi with live observability: context gauge, TPS, token/cost tracking, subscription usage bars, git status, and a full settings UI.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "pretest": "node ./tests/prepare-workspace-dependency.mjs",
    "test": "node --import ./tests/resolve-hook.mjs --test tests/*.test.ts",
    "typecheck": "tsc --noEmit --strict --allowImportingTsExtensions --moduleResolution bundler --module esnext --target esnext --skipLibCheck index.ts observability.ts lib/quota-provider.ts lib/footer-engine/*.ts lib/settings/*.ts lib/storage/*.ts"
  },
  "author": "Juan Benjumea",
  "keywords": [
    "pi-package",
    "pi-extension",
    "footer",
    "observability",
    "context",
    "usage",
    "tps",
    "coding-agent"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/huaxel/pi-dynamic-footer"
  },
  "bugs": {
    "url": "https://github.com/huaxel/pi-dynamic-footer/issues"
  },
  "homepage": "https://github.com/huaxel/pi-dynamic-footer#readme",
  "engines": {
    "node": ">=20.0.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": ">=0.80.0",
    "@earendil-works/pi-tui": "*"
  },
  "dependencies": {
    "@juanbenjumea/opencode-go-usage": "^0.3.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "files": [
    "index.ts",
    "observability.ts",
    "lib/",
    "README.md",
    "LICENSE"
  ]
}
